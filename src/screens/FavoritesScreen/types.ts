import {NativeStackScreenProps} from '@react-navigation/native-stack';
import {AppContainerParamList} from '../../navigators/AppContainer';

export interface FavoritesScreenProps
  extends NativeStackScreenProps<AppContainerParamList, 'Favorites'> {}
